header.header
    .header__column
        i.fab.fa-youtube
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="search by term...", name="term")
    .header__column
        ul
            li
                a(href="#") Join
            li
                a(href="#") Log in